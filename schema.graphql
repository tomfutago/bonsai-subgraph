type _Schema_
  @fulltext(
    name: "bonsaiSearch"
    language: en
    algorithm: rank
    include: [{
      entity: "Bonsai",
      fields: [
        { name: "name" }, { name: "pot" }, { name: "pet" }, { name: "bark" }, { name: "leaves" },
        { name: "fruit" }, { name: "numberOfFruit" }, { name: "groundCover" }, { name: "background" }
      ]
    }]
  )

type Bonsai @entity {
  id: ID!
  tokenID: BigInt!
  tokenURI: String!
  image: String!
  animation_url: String!
  name: String!
  pot: String
  pet: String
  bark: String
  leaves: String
  fruit: String
  numberOfFruit: String
  groundCover: String
  background: String
  createdAtTimestamp: BigInt!
  holder: Holder!
}

type Holder @entity {
  id: ID!
  bonsai: [Bonsai!] @derivedFrom(field: "holder")
}
